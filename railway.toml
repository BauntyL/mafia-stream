[build]
builder = "NIXPACKS"
buildCommand = "npm install && npm install --prefix client --include=dev && npm install --prefix server && npm run build --prefix client"

[deploy]
startCommand = "npm start --prefix server"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
