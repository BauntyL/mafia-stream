# Веб-Мафия для стримов

Онлайн-игра «Мафия» для стримеров: игроки заходят в браузере по коду лобби, а стример выводит красивую сетку камер в OBS.

[![Deploy to Render](https://render.com/images/deploy-to-render-button.svg)](https://render.com/deploy?repo=https://github.com/BauntyL/mafia-stream)

## Что умеет

- Лобби по коду, выбор ведущего, готовность игроков
- **Сценарий для ведущего**: готовый текст для чтения вслух на каждой фазе и индикатор «все походили — можно дальше»
- Роли: мирный житель, мафия, дон, шериф, доктор
- Ночные ходы с фиксацией: подтвердить выбор можно только один раз
- Тайный чат мафии ночью, общий чат днём, отдельная комната выбывших — **играть можно и без голосовой связи**
- Голосование с живым подсчётом голосов, воздержавшимися и переголосовкой при ничьей
- Таймеры обсуждения и речей по кругу
- Хроника партии и личная история проверок шерифа и дона
- Камеры игроков через VDO.Ninja — без установки программ; **без камеры тоже можно играть**, вместо видео будет портрет
- Отдельный экран `/overlay/КОД` для OBS: чистая сетка камер без интерфейса VDO.Ninja, счётчик голосов, таймер и карточки выбывших
- **Боты для теста**: ведущий добавляет их в лобби одной кнопкой, они сами ходят ночью, голосуют и переговариваются в чате — партию можно прогнать в одиночку

## Быстрый старт (локально)

```bash
npm install
npm install --prefix client
npm install --prefix server

npm run dev
```

- Сайт: http://localhost:5173
- Сервер: http://localhost:3001

## Как играть

1. Откройте сайт и введите никнейм.
2. Создайте лобби или войдите по коду.
3. В лобби выберите ведущего и при желании включите камеры.
4. Когда собралось минимум 6 игроков (не считая ведущего), ведущий начинает партию.
5. Ведущий читает сценарий на экране и жмёт «дальше», когда все походили.

Ведущий не получает роль и не голосует — он ведёт партию. Если игрок закрыл вкладку, он возвращается по тому же коду и нику.

## Роли по числу игроков

| Игроков | Мафия (с доном) | Шериф | Доктор | Мирные |
| ------- | --------------- | ----- | ------ | ------ |
| 6–8     | 2               | 1     | 1      | 2–4    |
| 9–11    | 3               | 1     | 1      | 4–6    |
| 12      | 4               | 1     | 1      | 6      |

## Проверка логики

Пока запущен `npm run dev`, можно прогнать партию автоматически:

```bash
node server/smoke-test.mjs   # 42 проверки правил на живых сокетах
node server/bot-test.mjs     # партия целиком на ботах, от раздачи до победы
```

## Настройка OBS

1. В лобби скопируйте ссылку «Экран OBS» — она выглядит как `https://ваш-сайт/overlay/КОД`.
2. В OBS: **Источники → + → Браузер**.
3. Вставьте ссылку, укажите размер **1920 × 1080**.
4. Включите «Обновить браузер при активации сцены».

Если видео не появляется, отключите аппаратное ускорение в OBS.

## Выкладка на Render (бесплатно)

1. Нажмите кнопку **Deploy to Render** выше.
2. Войдите или зарегистрируйтесь (можно через GitHub).
3. Render прочитает `render.yaml` и сам настроит сборку — нажмите **Apply**.
4. Через 3–5 минут получите адрес вида `https://mafia-xxxx.onrender.com`.

Особенности бесплатного тарифа: сервис засыпает после 15 минут полной тишины, первый вход после сна занимает около минуты. Пока идёт партия, соединения активны и сервис не засыпает. Лобби хранятся в памяти, поэтому после перезапуска сервера незавершённые партии пропадают.

## Структура

```
mafia/
├── client/       # React: лобби, игра, экран для OBS
├── server/       # Node.js + Socket.io: комнаты, роли, фазы
├── render.yaml   # Конфиг деплоя на Render
└── railway.toml  # Конфиг деплоя на Railway
```

## Технологии

React, Vite, TypeScript, Tailwind CSS, Framer Motion, Socket.io, Express, VDO.Ninja.

## Лицензия

MIT
